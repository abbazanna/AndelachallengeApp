<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/merriweather</item>
    </style>

    <style name="andelaButtonSytle" parent="AppTheme">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">25sp</item>
        <item name="android:background">@drawable/curved_button_border</item>
        <item name="android:layout_height">45dp</item>
    </style>

    <dimen name="profileTextSize">20sp</dimen>
    <dimen name="profileNameTextSize">25sp</dimen>

</resources>
